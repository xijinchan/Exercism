#if !defined(BOB_H)
#define BOB_H
#include <string>
#include <cctype>
#include <regex>

using std::string;
using std::regex;

namespace bob {
    string hey(string input);
}  // namespace bob

#endif // BOB_H