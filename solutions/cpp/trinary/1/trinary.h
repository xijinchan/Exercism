#if !defined(TRINARY_H)
#define TRINARY_H

#include <string>
#include <cmath>
#include <cctype>

using std::string;

namespace trinary {
    int to_decimal(string input);
}  // namespace trinary

#endif // TRINARY_H